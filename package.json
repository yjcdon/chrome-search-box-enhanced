{
  "name": "chrome-search-box-enhanced",
  "version": "1.0.0",
  "description": "Chrome 原生查找增强插件",
  "type": "module",
  "scripts": {
    "build": "vite build && cp manifest.json dist/ && node scripts/generate-icons.mjs",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
